apiVersion: v1
kind: Service
metadata:
  name: sonarqube-service
spec:
  selector:
    app: SONARQUBE
  type: NodePort
  ports:
    - name: sonarqube-http-port
      protocol: TCP
      port: 9000
      targetPort: 9000
      nodePort: 30900
